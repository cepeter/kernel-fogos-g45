--- a/KernelSU/kernel/selinux/sepolicy.c
+++ b/KernelSU/kernel/selinux/sepolicy.c
@@ -36,9 +36,11 @@
 static bool add_type_rule(struct policydb *db, const char *s, const char *t,
 			  const char *c, const char *d, int effect);
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 9, 0)
 static bool add_filename_trans(struct policydb *db, const char *s,
 			       const char *t, const char *c, const char *d,
 			       const char *o);
+#endif
 
 static bool add_genfscon(struct policydb *db, const char *fs_name,
 			 const char *path, const char *context);
@@ -497,7 +501,8 @@ static const struct hashtab_key_params filenametr_key_params = {
 	.cmp = filenametr_cmp,
 };
 #endif
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 9, 0)
 static bool add_filename_trans(struct policydb *db, const char *s,
 			       const char *t, const char *c, const char *d,
 			       const char *o)
@@ -559,6 +563,7 @@ static bool add_filename_trans(struct policydb *db, const char *s,
 
 	db->compat_filename_trans_count++;
 	return ebitmap_set_bit(&trans->stypes, src->value - 1, 1) == 0;
 }
+#endif
 
 static bool add_genfscon(struct policydb *db, const char *fs_name,
@@ -826,8 +831,12 @@ bool ksu_type_transition(struct policydb *db, const char *src, const char *tgt,
 			 const char *cls, const char *def, const char *obj)
 {
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 9, 0)
 	if (obj) {
 		return add_filename_trans(db, src, tgt, cls, def, obj);
 	} else {
+#endif
 		return add_type_rule(db, src, tgt, cls, def, AVTAB_TRANSITION);
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 9, 0)
 	}
+#endif
 }
